% Pas de section

\begin{frame}
    \frametitle{Pourquoi bootstrapper?\esp}

    \begin{tikzpicture}[out=90, in=-90]
        \node (ocaml_source) at (-1, 0) {
            \begin{minipage}{\textwidth}
                \mint{ocaml}|print_endline "Hello, World!";|
            \end{minipage}
        };

        \uncover<2->{
            \node (programme) at (-1, 2) {
                Programme exécutable
            };

            \node (ocamlc) at (3, 0) {
                \boxed{\texttt{ocamlc}}
            };

            \draw[-latex] (ocaml_source.north) to (programme.south);
            \draw[-latex] (ocamlc.north) to (programme.south);
        }

        \uncover<3->{
            \node (hand_written) at (-1, -2) {Écrit a la main};

            \draw [-latex] (hand_written.north) to (ocaml_source.south);
        }

        \uncover<4->{
            \node (bootstrap) at (3, -2) {
                \only<4>{?}
                \only<5->{\textbf{Bootstrap}}
            };

            \draw[-latex] (bootstrap.north) to (ocamlc.south);
        }
    \end{tikzpicture}

    \vspace{.5cm}

    \uncover<6->{
        \begin{itemize}
            \item Nécessité de pouvoir compiler le compilateur
            \item Archivage
            \item Vérification et sécurité
        \end{itemize}
    }
\end{frame}
